async function get(u){return (await fetch(u)).json()}function card(v){
  const youtubeId = v.youtube_id || '';
  const watchUrl = '/watch.html?id=' + encodeURIComponent(youtubeId);
  return `<article class="card" onclick="location.href='${watchUrl}'"><div class="thumb">${v.thumbnail ? `<img src="${escapeHTML(v.thumbnail)}" style="width:100%;height:100%;object-fit:cover">` : '▶'}</div><div class="body"><span class="meta">${v.access==='premium'?'🔒 PREMIUM':'FREE'} · ${escapeHTML(v.channel||'NIKHILVERSE')}</span><h3>${escapeHTML(v.title||'')}</h3><div class="meta">${escapeHTML(v.category||'Story')}</div></div></article>`;
}
async function load(){let vs=await get('/api/videos'),bs=await get('/api/blogs'),ps=await get('/api/plans');videoGrid.innerHTML=vs.map(card).join('')||'<p class=meta>No videos yet. Connect YouTube API in .env.</p>';docGrid.innerHTML=vs.filter(x=>(x.category||'').toLowerCase().includes('doc')).map(card).join('')||videoGrid.innerHTML;blogGrid.innerHTML=bs.map(b=>`<article class=blog><div class=img>${b.featured_image?`<img src="${b.featured_image}" style="width:100%;height:100%;object-fit:cover">`:''}</div><div class=body><span class=meta>${b.author||'Nikhil'}</span><h3>${b.title}</h3><p class=meta>${b.excerpt||''}</p></div></article>`).join('')||'<p class=meta>No articles yet.</p>';plans.innerHTML=ps.map(p=>`<div class=plan><span>${p.name}</span><h3>₹<strong>${p.price}</strong> / ${p.period}</h3><p>${p.features}</p><button onclick="alert('Razorpay checkout requires live credentials in .env')">Join Premium</button></div>`).join('')}function openAI(){ai.hidden=!ai.hidden}async function askAI(){let q=msg.value.trim();if(!q)return;chat.innerHTML='<b>You:</b> '+q+'<br><br>Thinking…';let r=await fetch('/api/ai',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:q})}).then(x=>x.json());chat.innerHTML='<b>You:</b> '+q+'<br><br>'+r.answer+(r.results?.length?'<br><br><b>Related:</b><br>'+r.results.map(x=>'• '+x.title).join('<br>'):'')}load();
